@model IEnumerable<GREENWICH.Models.IDEACATEGORY>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IDEA.AUTHOR)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IDEATAG.TITLE)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IDEA.AUTHOR)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IDEATAG.TITLE)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IDEACATEGORYID }) |
            @Html.ActionLink("Details", "Details", new { id=item.IDEACATEGORYID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IDEACATEGORYID })
        </td>
    </tr>
}

</table>
